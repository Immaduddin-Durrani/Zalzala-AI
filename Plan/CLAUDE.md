# Plan/CLAUDE.md

Context for this folder. See the root `CLAUDE.md` for project-wide rules.

## What's here
- `Task1_Workflow_Design.md` — the submitted Task 1 workflow design write-up.

## Folder-specific rules
- This is a submitted deliverable with a date on it. Don't edit it to match
  later implementation changes made after submission — if the design
  evolved since Task 1 was turned in (e.g. LangGraph instead of a plain
  loop, Juice Shop-only instead of DVWA/Juice Shop/VulnHub), note the delta
  in the Task 2/3 report instead of rewriting history here.
